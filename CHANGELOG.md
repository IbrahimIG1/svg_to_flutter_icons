## 0.1.4

- Add `--normalize-stroke` option for SVG cleaning mode.
- Normalize fixed `stroke` values to `currentColor` (while preserving `none`).
- Keep fill normalization and stroke normalization in one clean command.
- Update README/docs with stroke-related usage examples.

## 0.1.3

- Add package creators section to `README.md` so it is visible on pub.dev.

## 0.1.2

- Add DartDoc comments for public API members.
- Add creators section to `docs.md`.

## 0.1.1

- Initial version.
