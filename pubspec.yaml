name: svg_to_flutter_icons
description: CLI tool to clean SVGs, generate icon fonts, and create Flutter IconData classes.
version: 0.1.0
repository: https://github.com/yourname/svg_to_flutter_icons


environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  path: ^1.9.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1
executables:
  svg_to_flutter_icons:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

